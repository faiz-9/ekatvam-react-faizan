import React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "80vh",
      }}
    >
      <p style={{ fontSize: "24px", color: "grey" }}>NOT FOUND</p>
    </div>
  );
};

export default NotFound;
